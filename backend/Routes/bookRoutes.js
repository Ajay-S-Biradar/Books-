const express = require('express');

const router = express.Router();

// router.route('/').post(registerBook);
// router.route('/:id').get(retrieveBook).put(updateBook);
// router.route('/:id').delete(removeBook);

module.exports = router